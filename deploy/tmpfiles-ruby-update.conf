# Runtime dirs for the ruby HUD updater, installed to
# /etc/tmpfiles.d/ruby-update.conf. Queue is michael-writable so the
# unprivileged HUD can enqueue requests; everything else is root-only.
d /run/ruby-update 0755 root root -
d /run/ruby-update/queue 0770 michael michael -
